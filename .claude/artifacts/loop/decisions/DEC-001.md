---
id: DEC-001
title: Never-throws contract via Result return, source-breaking signature bump
status: accepted
introduced_by_slice: SLICE-01
introduced_at: 2026-07-08
related_specs: []
superseded_by: null
---
# DEC-001: Never-throws contract via Result return, source-breaking signature bump

## Context

gepa-core `TrialStore.put()` and `LockManager.acquire()` changed from throwing to returning `Result<T, PluginError>`. Source-breaking, but a repo-wide grep confirmed no in-repo callers outside the package's own tests. Bumped gepa-core 0.7.0→0.8.0 with a CHANGELOG entry rather than adding a non-throwing shim.


## Decision

The decision in declarative voice. "We will …"

## Consequences

### Positive

- Callers now get typed, discriminated errors (`E_TRIAL_SCHEMA` deterministic, `E_LOCK_WRITE` transient) instead of unhandled exceptions.
- First-consumer proof that `@astragenie/plugin-std` error taxonomy works before other repos adopt it.
- Any future external consumer of gepa-core 0.7.x must update call sites on upgrade.

### Negative

- bullet

### Neutral

- bullet

## Alternatives considered

- **Option A** — pros / cons / why rejected

## References

- Grade: `.claude/artifacts/loop/grades/SLICE-01-grade.md`
- Slice: `<slicesRoot>/.../SLICE_NN_*.md`
